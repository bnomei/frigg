# Cursor

- Point project rules at the AGENTS snippet template.
- Register Frigg as an MCP server; confirm `tools/list` includes `search_text`,
  `search_batch`, `workspace`.
- Soft shell justification can live in a user rule if shell tools compete with
  MCP on indexed paths.
